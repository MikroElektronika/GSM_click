unit Click_GSM_config;

uses Click_GSM_types;

const
    _GSM_TIMER_LIMIT : uint32_t = 5; // 5 ticks
    _GSM_BUF_WARNING : uint16_t = 192; // 192 bytes activate warning
    _GSM_POLL_ENABLE : uint8_t = 1; // poll enabled
    _GSM_CALLBACK_ENABLE : uint8_t = 0; // calback disabled
    _GSM_UART_CFG : array[6] of uint32_t = (9600, 0, _UART_8_BIT_DATA, _UART_NOPARITY, _UART_ONE_STOPBIT, _UART_HIGH_SPEED); 
    
end.